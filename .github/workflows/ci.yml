name: CI

on:
    push:
        branches:
            - main
    pull_request:

jobs:
    dioxus-bundle:
        uses: nigeleke/github/.github/workflows/dioxus-bundle.yml@main
        with:
            platform: desktop
