name: CI

on:
    push:
        branches:
            - main
    pull_request:

jobs:
    run-dioxus-ci:
        uses: nigeleke/github/.github/workflows/dioxus-bundle.yml@main
        with:
            platform: desktop
